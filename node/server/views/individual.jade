extends layout

block content
    .maincontent
        h1.has-icon.icon-happywhale= t("individual.meet", {individualId: data.ind.nickname + "(" + data.ind.id + ")"})

        if data.ind.photos
            //img.individual-main-image(src="#{config.wildbook.staticUrl}#{data.ind.photos[0]}")
    
            #individual-carousel.carousel.slide(data-ride="carousel" data-interval="5000")
                ol.carousel-indicators
                    li.active(data-target="#individual-carousel" data-slide-to="0")
                    - for (var ii = 1; ii < data.ind.photos.length; ii++)
                        li(data-target="#individual-carousel" data-slide-to="#{ii}")
                .carousel-inner
                    -var first = true
                    for photo in data.ind.photos
                        if first
                            - first = false
                            .item.active
                                img(src="#{config.wildbook.staticUrl}#{photo.url}" style="height: 400px; width: auto;")
                                if photo.caption
                                    .carousel-caption
                                        h3 photo.caption
                        else
                            .item
                                img(src="#{config.wildbook.staticUrl}#{photo.url}" style="height: 400px; width: auto;")
                                if photo.caption
                                    .carousel-caption
                                        h3 photo.caption

                <!-- Controls -->
                a.left.carousel-control(href="#individual-carousel" role="button" data-slide="prev")
                    span.glyphicon.glyphicon-chevron-left
                a.right.carousel-control(href="#individual-carousel" role="button" data-slide="next")
                    span.glyphicon.glyphicon-chevron-right
        h2= t("individual.contributors")
        hr
        for user in data.ind.submitters
            ul.submitters
                li
                    include partials/user
        hr
        h2= t("individual.recentActivity")
        hr
        
        div
            for encounter in data.ind.encounters
                ul.submitters
                    li #{encounter.verbatimLocation}
        p= t("individual.seenIndividual")
        a(href="/submitMedia")
            button.btn.btn-primary= t("individual.shareExperience")
        
        
block scripts
    script.
        //
        // Trigger bootstrap carousels
        //
        $(document).ready(function() {
            $('.carousel').carousel({
                interval: 3000
            });
        });
